interface中的方法是对service层的一层封装

service层提供阳光长跑、课程表、成绩等查询接口，为了系统的可靠性，同一功能可能有多种实现方式。
比如，在service层中，课表查询有强智api接口、教务系统爬虫两种实现方式，目的是防止有一天强智api关闭，可以通过爬虫提供服务。

interface层位于app之下，service之上，其意义在于当service发生变化是，对上层的app保持透明